import { Typography } from "@mui/material";

const Hero = () => {
  return (
    <div className="h-screen w-full flex flex-col justify-center items-center">
      <Typography variant="h1">Home Hero</Typography>
    </div>
  );
};

export default Hero;
